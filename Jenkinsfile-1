@Library('my-shared-lib') _

pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                buildApp()
                echo "Build stage completed"
            }
        }

        // stage('Docker') {
        //     steps {
        //         dockerBuild("myapp:1.0")
        //         echo "Docker image built successfull"
        //     }
        // }

        stage('Deploy') {
            steps {
                deployApp("prod")
                echo "Application deployed to production"
            }
        }
    }
}
